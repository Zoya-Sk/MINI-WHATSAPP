<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Messages</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>

<body class="bg-light">
    <div class="container py-5">
        <h1 class="text-center mb-4">📨 All Messages</h1>

        <div class="text-end mb-4">
            <a href="/messages/new">Send New Messages</a>
        </div>

        <div class="row g-4">
            <% messages.forEach(msg=> { %>
                <div class="col-md-6">
                    <div class="card shadow-sm">
                        <div class="card-body">
                            <h5 class="card-title">
                                <strong>
                                    <%= msg.from.username %>
                                </strong> ➡ <%= msg.to.username %>:
                            </h5>
                            <p class="card-text">
                                <%= msg.message %>
                            </p>

                            <small class="text-muted">Sent on: (<%= msg.createdAt.toLocaleString() %>)</small>

                            <!-- edit button -->
                            <div class="mt-3 d-flex gap-2">
                                <a class="btn btn-sm btn-outline-primary" href="/messages/<%= msg._id %>/edit">✍️
                                    Edit</a>
                                <form action="/messages/<%= msg._id %>?_method=DELETE" method="POST"
                                    style="display: inline">
                                    <button type="submit" class="btn btn-sm btn-outline-danger"
                                        onclick="return confirm('Delete this message?')">
                                        🗑️ Delete
                                </button>
                                </form>
                               
                            </div>
                             <!-- logout button -->
                                      <div class="text-end mb-3">
                                        <a href="/logout" class="btn btn-danger">🚪 Logout</a>
                                      </div>
                        </div>
                    </div>
                </div>
                <% }); %>
        </div>
    </div>
</body>

</html>